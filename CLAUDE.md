# 작업 지침 (Claude Code)

프로젝트 배경·아키텍처·Supabase 연결 방법은 [README.md](README.md)를 참고하세요.
이 파일은 이 저장소에서 작업할 때 지켜야 할 운영 규칙만 담습니다 — 어느
환경(노트북)에서 Claude Code를 열든 git으로 함께 따라오는 지침입니다.

## 커밋 · 푸시

**수정 후 커밋·푸시를 자동으로 하지 않습니다.** 사용자가 그 턴에 명시적으로
"커밋", "푸시"라고 요청했을 때만 git에 반영하세요. 검증까지는 알아서 진행해도
되지만, 이력에 남기는 건 항상 확인받습니다.

커밋 전에는 스테이지된 diff에 비밀값(Supabase anon key, DB 비밀번호, 공공데이터
API 키 등)이 섞여 들어가지 않았는지 반드시 훑어보세요. **이 환경의 셸에서
`grep`은 함수로 오버라이드돼 있어 특정 패턴 조합에서 조용히 에러를 내고 끝날
수 있습니다** — `grep ... || echo "안전"` 형태로 쓰면 grep이 실패해도 "안전"이
찍혀 false negative가 납니다. 보안 스캔처럼 결과를 신뢰해야 하는 곳에서는
`command grep`으로 실제 시스템 grep을 직접 호출하세요.

## 코드 변경 검증

- 타입 체크: `npx tsc --noEmit`
- 빌드: `npm run build`
- UI가 걸린 변경은 로컬 dev 서버(`npm run dev`, Browser 프리뷰 도구)에서 실제로
  띄워 확인하세요. 모바일 전용 동작(예: 파일 입력의 `capture` 속성으로 카메라
  강제 실행, 터치 제스처)은 데스크톱 프리뷰로 재현이 안 되니 한계를 사용자에게
  분명히 알리세요.
- Supabase를 쓰는 흐름을 확인할 때는 `<실제이메일>+verifyN@gmail.com` 같은
  일회용 테스트 계정을 새로 만들어 쓰세요(회원가입은 `supabase.auth.signUp()`을
  브라우저 콘솔에서 직접 호출하는 편이 폼 자동화보다 안정적이었습니다). 확인이
  끝나면 그 계정과 연결된 행만 정확히 지정해 SQL로 삭제하고, 삭제 후 실제
  사용자의 데이터는 그대로인지 다시 확인하고 마칩니다.

## 스키마 변경

`supabase/migrations/`에 타임스탬프 파일로만 추가합니다 — 과거 마이그레이션은
절대 수정하지 않습니다. `supabase/schema.sql`도 같은 내용으로 함께 갱신해 최신
스키마 스냅샷을 유지하세요. 적용은 `npx supabase db push --linked`.

## 알아두면 좋은 것

- 이 프로젝트는 이전엔 "하루트립"이었고 지금은 **"하루여행"**입니다 — 사용자
  대면 텍스트에 옛 이름이 남아 있으면 고쳐야 할 대상입니다.
- 모바일 사용이 압도적으로 많은 서비스입니다. 리스트 순서 변경 같은 조작은
  드래그 앤 드롭보다 버튼(▲▼) 방식을 선호합니다 — 모바일에서 드래그가 화면
  스크롤 제스처와 자주 충돌해서입니다.
- 방문 인증 사진 첨부는 파일 입력에 `capture` 속성을 강제로 넣지 않습니다 —
  카메라 권한이 꺼져 있으면 아무 반응 없이 조용히 실패하는 기기가 있어, 대신
  브라우저 기본 파일 선택창(OS가 촬영/앨범 선택지를 직접 보여줌)을 씁니다.
